   <% @articulos.each do |articulo| %>
      <tr class="row-<%=articulo.interno%>">
      <td><%= articulo.interno %></td>
      <td><%= articulo.codigo %></td>
      <td><%= articulo.descripcion %></td>
      <td><%= articulo.rubro.nombre[0..1] %></td>
      <td><%= mostrarPrecio(articulo.precio) %></td>
      <td><%= mesAnio(articulo.fecha_precio) %></td>
      <td><%= articulo.ubicacion %></td>
      <td>
      <%= link_to "Editar", edit_articulo_path(articulo), remote: true, class: 'btn btn-modificar btn-sm'%>
      <%= link_to "Borrar", delete_path(articulo), remote: true, class: 'btn btn-danger btn-eliminar btn-sm'%>
      </td>
      </tr>
   <% end %>
