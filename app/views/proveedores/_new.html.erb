   <div id="new-proveedor-modal" class="modal fade">
  		<div class="modal-dialog modal-md">
    	 <div class="modal-content">
    	  <div class="modal-header">
       		<h5 class="modal-title">Nuevo proveedor</h5>
        		<button type="button" class="close" data-dismiss="modal" aria-label="close">
          		<span aria-hidden="true">&times;</span>
        	</button>
	      </div>
			<div>
			    <%= form_for @proveedor,  url: {action: "create"} do |p| %>
			    <div class="modal-body">
			    	<div class="form-control-inline">
						<%= p.label :'Número',  :class => "col-sm-2" %>
			  			<%= p.text_field :id, :readonly => true, :class => "col-md-3"%>
			 		</div>
					<div class="form-control-inline">
						<%= p.label :'Nombre', :class => "col-sm-2" %>
			  			<%= p.text_field :nombre, :class => "col-md-7"%>
			 		</div>
			 	</div>

				    <div class="modal-footer">
				    	<%= p.submit "Crear nuevo proveedor", class: "btn btn-edit btn-create"%>
				    	<button type="button" class="btn btn-secondary" data-dismiss="modal">Cerrar</button>
					</div>
				
				<% end %>

		 	</div>
		 </div>
     </div>